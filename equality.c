#include<stdio.h>
int main ()
{int a,b;
printf ("Enter first no.:");
    scanf ("%d",&a);
    printf ("Enter second no.:");
    scanf ("%d",&b);
   /* if (!(a < b) && !(a > b )){
        printf ("Both no. are equal\n");
    }*/
    else {
        printf (" No. are not equal");
    }
    return 0;
}